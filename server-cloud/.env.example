# Réseau Docker existant sur le VPS
# Trouver le nom avec : docker network ls
DOCKER_NETWORK=vpn_net

# PostgreSQL
PG_HOST=172.18.0.3
PG_PORT=5432
PG_DATABASE=doudoutacos
PG_USER=admin
PG_PASSWORD=admin

# Clé API (requise sur tous les appels /api/* depuis le POS)
# Générer avec : openssl rand -hex 32
API_KEY=change_me_avec_une_vraie_cle_secrete
